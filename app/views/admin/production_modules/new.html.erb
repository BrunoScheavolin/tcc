<div class="container d-flex justify-content-center">
  <div class="card p-3 shadow-sm rounded w-100 mx-auto" style="max-width: 900px; background-color: white;">
    <% if @production_module.module_type == "tasks" %>
      <h4>Novo Módulo Geral</h4>
      <%= render "form_tasks", production_module: @production_module %>
    <% else %>
      <p class="text-danger">Tipo de módulo inválido</p>
    <% end %>
  </div>
</div>
