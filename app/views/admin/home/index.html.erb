<style>
    .cover-img {
        width: 100%;
        height: 300px; /* Altura padrão */
        object-fit: cover;
        object-position: center;
    }

    @media (max-width: 768px) { /* Ajuste para telas menores */
        .cover-img {
            height: 200px; /* Reduz altura */
        }
    }
</style>

<img src="<%= asset_path('campo2.png') %>" class="cover-img">
